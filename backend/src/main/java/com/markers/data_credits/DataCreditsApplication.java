package com.markers.data_credits;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Punto de entrada de la API data_credits de gestión de préstamos bancarios.
 */
@SpringBootApplication
public class DataCreditsApplication {

    public static void main(String[] args) {
        SpringApplication.run(DataCreditsApplication.class, args);
    }
}
